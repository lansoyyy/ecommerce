class ModelBestSell {
  String image;
  String name;
  String price;
  String totalView;
  String view;
  bool select;

  ModelBestSell(this.image, this.name, this.price, this.totalView, this.view,
      this.select);
}
